- name: Insomnia
  hosts: 127.0.0.1
  connection: local

  tasks:
    - name: Install rust
      shell:
        cmd: "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --no-modify-path -y"